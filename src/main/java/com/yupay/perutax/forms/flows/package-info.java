/**
 * Package with data flows.
 * A data flow is an abstraction over reactive
 * non-blocking operations on databases, such
 * as select queries; and over common DAO blocking
 * operations like inserts and updates.
 *
 * @author InfoYupay SACS
 * @version 1.0
 */
package com.yupay.perutax.forms.flows;